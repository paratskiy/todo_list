%div{class: "task-name-#{task.id}"}
  %ul{style: "color: red"}
    %li
      = task.name
      = link_to "edit", edit_task_path(task), remote: true
      = link_to "delete", task, method: :delete, data: { confirm: "You sure?" }, remote: true